export interface CovidModel {
  confirmed: number;
  confirmed_daily: number;
  deaths: number;
  deaths_daily: number;
  recovered: number;
  recovered_daily: number;
  date: string;
  population: number;
}
